<?php
if ( ! defined( 'ABSPATH' ) ) exit; $wpc_gateways = $this->cc_get_settings( 'gateways' ); if ( !empty( $wpc_gateways['allowed'] ) && in_array( 'stripe', $wpc_gateways['allowed'] ) ) { $new_notices['stripe_update_lib_2017_06_05'] = 'Stripe API library was updated to the latest version 2017-06-05. Please login to your Stripe account and verify that you are running the latest Stripe API version.' . '<br> See more details <a target="_blank" href="https://dashboard.stripe.com/account/apikeys">here</a>.'; $this->add_wpc_notices( $new_notices ); } global $wp_roles; $capability_map = array_merge( array_values( $this->get_map_caps( 'clientspage' ) ), array_values( $this->get_map_caps( 'portalhub' ) ) ); foreach ( $capability_map as $capability ) { $wp_roles->add_cap( 'administrator', $capability ); $wp_roles->add_cap( 'wpc_admin', $capability ); $wp_roles->add_cap( 'wpc_manager', $capability ); } $this->reset_rewrite_rules();